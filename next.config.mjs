/**
 * [ไฟล์] next.config.mjs
 * -----------------------------------------------------------------------
 * ค่าตั้งต้นของ Next.js ยังไม่ได้ปรับแต่งอะไรเป็นพิเศษ
 * ถ้าอนาคตอยาก deploy แบบ static export ล้วน (ไม่ใช้ฟีเจอร์ server)
 * สามารถเพิ่ม output: "export" ที่ object นี้ได้
 * -----------------------------------------------------------------------
 */
/** @type {import('next').NextConfig} */
const nextConfig = {};
export default nextConfig;
